<view class="customs-page">
  <view class="bg-watermark" aria-hidden="true">
    <!-- WXML 不支持 svg 标签，图形放在 wxss 里做背景图 -->
    <view class="watermark-svg"></view>
  </view>

  <scroll-view class="customs-scroll" scroll-y="true" show-scrollbar="false">
    <view class="hero-section">
      <view class="back-btn" bindtap="onGoBack">
        <text class="back-arrow">←</text>
      </view>
      <view class="lang-tag">{{languageLabel}}</view>
      <text class="hero-title">{{pageTitle}}</text>
    </view>

    <block wx:if="{{hasData}}">
    <view class="bento-grid">
      <view class="card card-half">
        <text class="card-title">行为红线</text>
        <view class="bullet-row" wx:for="{{behaviorLines}}" wx:key="*this">
          <view class="icon-wrap" aria-hidden="true">•</view>
          <text class="bullet-text">{{item}}</text>
        </view>
      </view>

      <view class="card card-half">
        <text class="card-title">颜色避雷</text>
        <view class="swatch-row">
          <view class="swatch swatch-white"></view>
          <view class="swatch swatch-blue"></view>
          <view class="swatch swatch-black"></view>
        </view>
        <text class="swatch-text">{{colorText}}</text>
      </view>
    </view>

    <view class="card card-full">
      <text class="card-title">宗教禁忌（必看）</text>
      <view class="bullet-row" wx:for="{{religionLines}}" wx:key="*this">
        <view class="icon-wrap" aria-hidden="true">•</view>
        <text class="bullet-text">{{item}}</text>
      </view>
    </view>

    <view class="card card-full dress-card">
      <text class="card-title">着装建议</text>
      <view class="dress-layout">
        <view class="dress-col">
          <text class="dress-title">男士 👔</text>
          <text class="dress-text">{{dressMen}}</text>
        </view>
        <view class="dress-col">
          <text class="dress-title">女士 👗</text>
          <text class="dress-text">{{dressWomen}}</text>
        </view>
      </view>
    </view>

    <view class="card card-full">
      <text class="card-title">社交礼仪</text>
      <view class="chip-row">
        <text class="chip" wx:for="{{etiquetteChips}}" wx:key="*this">{{item}}</text>
      </view>
      <text class="etiquette-text">{{etiquetteText}}</text>
    </view>

    <view class="card card-full">
      <text class="card-title">特殊习俗（当地高频）</text>
      <view class="sub-title">当地习俗</view>
      <text class="bullet-text">{{customText}}</text>
    </view>

    <view class="card card-full">
      <text class="card-title">官方信息来源</text>
      <text class="source-line" wx:for="{{sourceLines}}" wx:key="*this">{{item}}</text>
    </view>
    </block>

    <view wx:else class="customs-empty">
      <text class="customs-empty-icon">🌍</text>
      <text class="customs-empty-title">暂无该国家风俗资料</text>
      <text class="customs-empty-desc">我们不会把其他国家的禁忌当作本地建议，请等待来源核验后更新。</text>
    </view>

    <view class="page-gap"></view>
  </scroll-view>
</view>
<elephant-pet />
