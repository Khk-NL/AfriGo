# 安全性与远程服务改进清单

## 当前决策

现阶段优先完善产品功能和核心用户流程。本项目已确定使用 Express + MySQL 作为唯一业务后端、阿里云 ECS 作为远程运行环境、阿里云 OSS 保存媒体文件，并使用 ISO 3166-1 两位国家代码作为稳定关联字段。

功能开发期间，后端仅用于本机或受控测试环境，不对公网开放。准备真机联调、体验版或正式发布前，必须完成下方 P0 项目。

## 当前已知问题

### 身份认证与权限

- `wx.login` 获取的临时 `code` 尚未发送到可信服务端换取真实 OpenID。
- 当前登录接口接受客户端传入的 OpenID，身份可以被伪造。
- 管理员身份会读取客户端本地缓存，不能作为可信权限依据。
- 后端新增、修改、删除、上传等接口缺少统一的服务端鉴权和角色校验。
- 用户登录态没有可靠的 session/token、过期和撤销机制。

### 远程服务

- 小程序 API 地址写死为 `http://127.0.0.1:3001`，只能用于本机模拟器环境。
- 尚未区分开发、测试和生产环境。
- 没有 HTTPS 部署、微信小程序合法域名和上传域名配置说明。
- 历史微信云函数目录仍保留在仓库中，但不再继续扩展；业务后端统一使用 Express/MySQL。
- 缺少 `.env.example`、部署步骤、数据库备份与恢复说明。

### 接口与数据安全

- CORS 当前没有限制可信来源。
- 上传接口需要补充文件 MIME、扩展名、数量和内容校验。
- 缺少请求频率限制、参数 Schema 校验、统一错误处理和安全日志。
- 服务端错误信息可能直接返回给客户端，不适合生产环境。
- 社区内容缺少审核、举报、删除留痕和敏感内容处理机制。

## P0：体验版或公网部署前必须完成

- [x] 确定唯一后端方案：Express/MySQL。
- [x] 完成 `wx.login -> code2session -> 服务端登录态` 的可信登录代码链路。
- [x] 所有现有写接口增加服务端鉴权；管理员操作增加服务端 RBAC 校验。
- [x] 客户端不再自行提供或决定 OpenID、role、isAdmin 等可信字段。
- [ ] 使用 HTTPS 部署 API，并配置微信 request/uploadFile 合法域名。
- [x] 将 API 地址改为环境配置，区分 development 和非开发环境。
- [ ] 配置正式 API 域名并写入小程序远程环境配置。
- [ ] 限制 CORS、上传类型和上传大小，并增加基础限流。
- [x] 上传类型和大小已限制，媒体由服务端上传至私有阿里云 OSS。
- [x] 秘钥、数据库密码和 AppSecret 只通过环境变量或 secrets 管理，不进入 Git。
- [ ] 为数据库建立备份、恢复和最小权限账号。

## P1：正式发布前完成

- [ ] 使用 Schema 校验登录、帖子、景点、推荐和管理接口参数。
- [ ] 统一错误码和客户端提示，生产环境不返回数据库或内部异常细节。
- [ ] 增加登录失败、越权、删除、上传和内容审核等安全日志。
- [ ] 社区增加审核状态、举报入口、敏感内容处理和管理员操作记录。
- [ ] 为政策、签证、安全和健康内容保存来源、发布日期、复核时间和审核人。
- [ ] 增加接口集成测试，覆盖未登录、普通用户、管理员和越权请求。
- [ ] 补充隐私政策、用户数据说明、账号注销和数据删除流程。

## 远程服务建议方案

已确定继续使用现有 Express/MySQL，并部署到阿里云服务器：

1. 使用环境变量配置数据库、端口、公开域名和微信 AppSecret。
2. 通过 Nginx 或云平台提供 HTTPS，Node 服务只监听内网端口。
3. 小程序用 `wx.request` 和 `wx.uploadFile` 访问同一正式 API 域名。
4. 服务端使用微信登录凭据识别用户，并签发短期登录态。
5. 数据库使用非 root 账号，定期备份，并限制外网访问。

历史微信云函数代码暂时保留用于对照，后续确认没有依赖后再清理，不再形成第二套业务实现。

## 进入安全改造阶段的触发条件

以下核心功能闭环后，开始处理本清单：

- [ ] 国家选择后，各内容页能正确展示对应国家数据。
- [ ] 登录、发布、社区列表和个人中心形成可演示闭环。
- [ ] 图片选择、上传、保存和社区展示形成完整链路。
- [ ] 收藏和消息不再只是静态占位展示。
- [ ] 主要页面交互在微信开发者工具中编译通过。

## 验收标准

- 客户端修改本地缓存不能获得管理员权限。
- 伪造 OpenID、role 或 isAdmin 的请求会被服务端拒绝。
- 未登录用户不能调用任何受保护的写接口。
- 真机可通过 HTTPS 访问 API 并上传图片。
- 仓库中不存在 AppSecret、数据库密码、token 或私钥。
- 安全与权限相关集成测试全部通过后，才允许对公网开放。
